# Scenario 3: boundary

**目的**: プレイヤーがフィールド境界に到達しようとしたとき、座標が正しくクランプ（止められる）されることを検証します。

- 初期位置は `(-50, 0)`（北側は Y 正方向）
- `move(600)` を実行し、Y座標がフィールド上限 `+500` にクランプされることを期待します。
- X座標は変化しません。
- プレイヤーの角度は変わらず `0°` のままです。
