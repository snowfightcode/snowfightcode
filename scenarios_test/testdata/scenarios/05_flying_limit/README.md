# Scenario 5: flying_limit

**目的**: 飛行中の雪玉数が `max_flying_snowball=3` を超えないことを検証します。

- P1 は毎ティック雪玉を投げようとします（`toss(90, 100)`）
- `max_flying_snowball=3` の制限により、同時に飛行できる雪玉は最大3個まで
- 4個目以降の投擲は無視されます
- テストでは飛行中の雪玉数が3個を超えないことを確認します
